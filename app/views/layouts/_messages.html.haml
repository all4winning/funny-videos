- flash.each do |name, msg|
  %div{:class => "alert alert-#{[:error, :alert].include?(name) ? "error" : "success"}"}
    %a.close{"data-dismiss" => "alert"} ×
    = content_tag :div, msg, :id => "flash_#{name}" if msg.is_a?(String)